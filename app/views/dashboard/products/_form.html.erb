<%= form_for([:dashboard, @product]) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label "Nome", class:"col-sm-2 control-label" %>
    <div class="col-sm-7">
      <%= f.text_field :name, class: "form-control" %>
    </div>
  </div>


  <div class="form-group">
    <%= f.label "Descrição", class:"col-sm-2 control-label" %>
    <div class="col-sm-7">
      <%= f.text_field :desc, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label "Valor Minimo", class:"col-sm-2 control-label" %>
    <div class="col-sm-7">
      <%= f.text_field :min_donation, class: "form-control" %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
